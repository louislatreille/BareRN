import React, { Component } from 'react';
import ScreensStack from './config/Router';

export default class BareRN extends Component {
    render() {
        return (
            <ScreensStack/>
        )
    }
};